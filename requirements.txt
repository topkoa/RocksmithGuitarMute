# Core dependencies for RockSmith Guitar Mute
# Updated versions to match working local environment
torch==2.6.0  # Pinned: torchaudio 2.7+ requires torchcodec which is not available on Windows
torchaudio==2.6.0  # Pinned: 2.7+ switches default backend to torchcodec (Windows incompatible)
numpy>=1.26.4  # Use available binary wheels
demucs>=4.0.1,<4.1.0  # Use stable 4.0.1 instead of alpha 4.1.0a3
soundfile>=0.13.1
cffi>=1.0

# Critical PyTorch/Demucs dependencies
filelock>=3.0
fsspec
jinja2>=3.0
markupsafe>=2.0
mpmath>=1.1.0,<1.4  # torch 2.6.0 requires mpmath<1.4
networkx>=3.0
sympy==1.13.1  # Pinned: torch 2.6.0 requires exactly 1.13.1
pillow>=10.0

# Dependencies for rs-utils (Python scripts for Rocksmith 2014)
construct>=2.5.1
docopt>=0.6.1
pycryptodome>=3.4.11
six>=1.5.2

# Dependencies for rsrtools (Rocksmith save files and songlists)
simplejson>=3.19.2
tomli-w>=1.0.0
pydantic>=2.6.1

# Additional demucs dependencies
einops
diffq
omegaconf>=2.0.0
hydra-colorlog>=1.1
hydra-core>=1.1.0
julius>=0.2.3
pyyaml
tqdm
dora-search
lameenc>=1.2
openunmix

# GUI dependencies
# tkinter is included with Python, no need to install separately

# Build dependencies for standalone executable
pyinstaller>=5.0.0

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0